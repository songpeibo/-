package com.sort.sort_abstract;

import java.util.ArrayList;

public abstract class Sort_Abstract {
    abstract public int size(Object []objects);
    abstract public boolean lessThan(Object o1,Object o2);
    abstract public void swap(Object o1,Object o2);
}
